[project]
name = "jkkwatcher"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "tokyo3am", email = "crusher.plebes.3a@icloud.com" }
]
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "httpx>=0.28.1",
    "lxml>=6.1.1",
    "rich>=15.0.0",
    "typer>=0.25.1",
]

[project.scripts]
jkkwatcher = "jkkwatcher:main"

[build-system]
requires = ["uv_build>=0.10.7,<0.11.0"]
build-backend = "uv_build"
